CREATE DATABASE testDatabase;

use testDatabase;

CREATE TABLE testTable(
    id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    testColumn_first VARCHAR (255) NULL,
    testColumn_second VARCHAR (255) NULL
);